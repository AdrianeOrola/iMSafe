# Sliding response journey and image removal

User-directed refinement: no AI imagery on any page, no picture in tracking, no visible Pause motion button, and no homepage Community response / Disaster response illustration labels. The hero now reuses the existing shield symbol. The generated raster is unused and retained for recovery only.

The Observe / Report / Coordinate / Track strip loops horizontally with a 32-second linear CSS transform. This is a homepage explanatory sequence, not motion on emergency form controls. Duplicates are hidden from assistive technology. Hover/focus holds the strip; click or Enter/Space toggles a persistent stop state without adding a separate visible button. It stops offscreen or in hidden tabs. Reduced-motion and no-JavaScript modes show a static complete sequence.

Verification: desktop and mobile screenshots reviewed together; no layout corrections needed. Public-route browser suite passed at 1440/390px with overflow checks at 320/720px, plus actual transform movement, keyboard control, offscreen stopping, reduced motion, image/label removal, Cavite/NCR location cascades, rejected-report restoration, and retry. PHP and JavaScript syntax checks passed for the main edited files.

Detector findings on changed UI: 66 color, 8 font-size, and 2 radius advisories against the older design notes; no other finding categories. Existing DESIGN.md and its sidecar were not changed by this scoped refinement.

Verdict: requested changes implemented and browser-tested. Location provider and backend behavior were not changed in this turn.
