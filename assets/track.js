const trackForm=document.getElementById('trackForm');
if(trackForm){trackForm.addEventListener('submit',event=>{const input=document.getElementById('reference');const validation=document.getElementById('trackValidation');const value=input.value.trim().toUpperCase();input.value=value;if(!/^IMS-[A-Z0-9]+-[A-Z0-9]+$/.test(value)){event.preventDefault();validation.hidden=false;validation.textContent='Enter a valid iMSafe v2.0 report reference, such as IMS-ABC123-XYZ12.';input.focus();return}validation.hidden=true;const button=trackForm.querySelector('button');button.disabled=true;button.querySelector('span').textContent='Checking…';button.classList.add('is-loading')})}
